import React from "react";
import { useEffect, useState } from "react";

import { HeroMedia } from "../components/HeroMedia";

export const MediaPage = ({ media_id }) => {

  useEffect(() => {}, []);

  return (
    <div>
      {/* {media ? (
        <HeroMedia baseMedia={media} />
      ) : (
        <div className="text-font-50">Media</div>
      )} */}
    </div>
  );
};
